<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.todo.mapper.TodoMapper">

    <resultMap id="TodoResultMap" type="com.todo.model.Todo">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="startTime" column="start_time"/>
        <result property="dueDate" column="due_date"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUserId" resultMap="TodoResultMap">
        SELECT id, user_id, title, content, status, start_time, due_date, created_at, updated_at
        FROM todos
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="findById" resultMap="TodoResultMap">
        SELECT id, user_id, title, content, status, start_time, due_date, created_at, updated_at
        FROM todos
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.todo.model.Todo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todos (user_id, title, content, status, start_time, due_date, created_at, updated_at)
        VALUES (#{userId}, #{title}, #{content}, #{status}, #{startTime}, #{dueDate}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.todo.model.Todo">
        UPDATE todos
        SET title = #{title},
            content = #{content},
            status = #{status},
            start_time = #{startTime},
            due_date = #{dueDate},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM todos
        WHERE id = #{id}
    </delete>

    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM todos
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM todos
        WHERE user_id = #{userId}
    </delete>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO todos (user_id, title, content, status, start_time, due_date, created_at, updated_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.title}, #{item.content}, #{item.status}, #{item.startTime}, #{item.dueDate}, #{item.createdAt}, #{item.updatedAt})
        </foreach>
    </insert>

    <update id="updateOverdueStatus">
        UPDATE todos
        SET status = 'INCOMPLETE', updated_at = NOW()
        WHERE user_id = #{userId}
          AND due_date &lt; NOW()
          AND status = 'PENDING'
    </update>

</mapper>
